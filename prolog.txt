factorial query= fact(5,X).
fact(0,1).

fact(N,F):-
    N>0,
    N1 is N-1,
    fact(N1,F1),
    F is N*F1.


length of list query= len([a,b,c,d],X).
len([],0).

len([_|T],N):-
    len(T,N1),
    N is N1+1.


reverse list query= rev([1,2,3,4],X).
rev([],[]).

rev([H|T],R):-
rev(T,R1),
append(R1,[H],R).